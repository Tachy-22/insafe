import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "InSafe - Insider Threat Monitoring | Wema Bank Nigeria",
  description: "Advanced insider threat monitoring and security platform for Wema Bank Nigeria",
  keywords: "insider threat, security monitoring, Wema Bank, fraud detection, data protection",
  authors: [{ name: "Wema Bank Nigeria" }],
  icons: {
    icon: "/wema-logo.jpeg",
    apple: "/wema-logo.jpeg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
